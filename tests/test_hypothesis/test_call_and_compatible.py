# This test code was written by the `hypothesis.extra.ghostwriter` module
# and is provided under the Creative Commons Zero public domain dedication.




# needs to be some real tools, not just random strings.
# @given(
#     tool_info=st.from_type(
#         tuple[str, cli_tool_audit.models.CliToolConfig, DummyLock, bool]
#     )
# )
# def test_fuzz_check_tool_wrapper(
#     tool_info: tuple[
#         str, cli_tool_audit.models.CliToolConfig, DummyLock, bool
#     ]
# ) -> None:
#     cli_tool_audit.call_and_compatible.check_tool_wrapper(tool_info=tool_info)
